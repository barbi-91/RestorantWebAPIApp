using Microsoft.AspNetCore.Mvc;

namespace AbySalto.Junior.Controllers
{
    public class RestaurantController : Controller
    {
        /* 
           The project is set up as a Web API (Program.cs, AddControllers, Swagger), 
           so all functional implementation is done in OrderController (DRY, KISS, YAGNI).
        */
    }
}
